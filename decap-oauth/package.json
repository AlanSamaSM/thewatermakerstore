{
  "name": "decap-oauth",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub OAuth proxy for Decap CMS on thewatermakerstore",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
